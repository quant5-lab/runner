Codegen limitation: ta.crossover/crossunder in if condition generates undefined series variables

Parse: ✅ Success
Generate: ✅ Success
Compile: ❌ Fails (undefined: ta_crossover_XXXSeries, ta_crossunder_XXXSeries)
Execute: ❌ Not reached

Issue: Codegen generates series variable references but doesn't declare/initialize them
Expected: Use inline IIFE pattern like other crossover cases
Blocker: Prevents testing strategy.entry/close comment parameter functionality
