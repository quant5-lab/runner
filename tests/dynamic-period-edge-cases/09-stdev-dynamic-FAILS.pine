//@version=5
// TEST: stdev() with dynamic period - FAILS (BLOCKER #18)
// Error: "ta.stdev period must be literal"
indicator("Dynamic period: ta.stdev(close, getPeriod())")
getPeriod() => 14
plot(ta.stdev(close, getPeriod()))
