//@version=5
indicator("For Loop Zero Step Test", overlay=false)

// Validates runtime panic on zero step to prevent infinite loop
result = 0.0
for i = 1 to 10 by 0
	result := result + i

plot(result, "Zero Step", color=color.red)
