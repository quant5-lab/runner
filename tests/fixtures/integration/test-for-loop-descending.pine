//@version=5
indicator("For Loop Descending Test", overlay=false)

// Expected: 120.0 (5 factorial via descending loop with negative step)
product = 1.0
for i = 5 to 1 by -1
	product := product * i

plot(product, "Product 5-1", color=color.red)
