//@version=5
indicator("For Loop Single Iteration Test", overlay=false)

// Expected: 10.0 (single iteration edge case where from == to)
val = 0.0
for i = 5 to 5
	val := i * 2

plot(val, "Single Iteration", color=color.orange)
